// Primeiro Aluno
var studentName = "João da Silva";
var firstScore = 7;
var secondScore = 8.5;

var firstScoreMultiplied = firstScore * 0.6;
var secondScoreMultiplied = secondScore * 0.4;

var finalScore = firstScoreMultiplied + secondScoreMultiplied;

alert("Nome do Primeiro Aluno: " + studentName + "\n" + "Nota 1: " + firstScore.toFixed(2) + "\n" + "Nota 2: " + secondScore.toFixed(2) + "\n" + "Nota Final: " + finalScore.toFixed(2));

// Segundo Aluno
studentName = "Nicolas Silveira";
firstScore = 9;
secondScore = 9.5;

firstScoreMultiplied = firstScore * 0.6;
secondScoreMultiplied = secondScore * 0.4;

finalScore = firstScoreMultiplied + secondScoreMultiplied;

alert("Nome do Segundo Aluno: " + studentName + "\n" + "Nota 1: " + firstScore.toFixed(2) + "\n" + "Nota 2: " + secondScore.toFixed(2) + "\n" + "Nota Final: " + finalScore.toFixed(2));

// Terceiro Aluno
studentName = "Zé Buscapé";
firstScore = 2;
secondScore = 5.75;

firstScoreMultiplied = firstScore * 0.6;
secondScoreMultiplied = secondScore * 0.4;

finalScore = firstScoreMultiplied + secondScoreMultiplied;

alert("Nome do Terceiro Aluno: " + studentName + "\n" + "Nota 1: " + firstScore.toFixed(2) + "\n" + "Nota 2: " + secondScore.toFixed(2) + "\n" + "Nota Final: " + finalScore.toFixed(2));